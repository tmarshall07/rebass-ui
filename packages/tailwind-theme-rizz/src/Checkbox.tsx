import React, { PropsWithRef } from 'react';
import { BaseCheckbox, CheckboxProps } from '@tannerjs/tailwind-base';

export const Checkbox = (props: PropsWithRef<CheckboxProps>) => <BaseCheckbox {...props} />;
