import React from 'react';
import { Switch as _Switch, SwitchProps } from '@rebass/forms/styled-components';

const Switch = (props: SwitchProps) => {
  return <_Switch {...props} />;
};

export default Switch;
